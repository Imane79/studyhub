{
  "id": "sec-6-4",
  "number": "8.5.2+",
  "title": "DCT vs DFT, Matrix Form & JPEG Pipeline",
  "content": [
    {
      "type": "markdown",
      "body": "## 2D DCT (for images)\n\n$$F(u,v) = \\frac{C(u)C(v)}{4} \\sum\\sum \\cos\\left(\\frac{(2i+1)u\\pi}{16}\\right) \\cos\\left(\\frac{(2j+1)v\\pi}{16}\\right) f(i,j)$$\n\nApplied to **8×8 blocks** → produces **64 coefficients**\n\n```\nTop-left  (0,0): DC — average brightness\nMoving right  →: higher horizontal frequency\nMoving down   ↓: higher vertical frequency\nBottom-right (7,7): highest frequency both directions\n```\n\n**Building signal step-by-step:** DC → + AC1 → + AC2 → ... → all 64 = perfect reconstruction\n\n---\n\n## Matrix Form of DCT\n\nInstead of the full 2D formula (64 iterations), do **two 1D DCTs** (rows then columns) — only 16 iterations.\n\n$$F = T \\times f \\times T^T \\qquad \\text{(DCT: pixels → frequency)}$$\n\n$$f = T^T \\times F \\times T \\qquad \\text{(IDCT: frequency → pixels)}$$\n\n$$T[i,j] = \\begin{cases} \\frac{1}{2\\sqrt{2}} & i=0 \\\\ \\frac{1}{2}\\cos\\left(\\frac{(2j+1)i\\pi}{16}\\right) & i>0 \\end{cases}$$\n\n**KEY PROPERTY:** $T^T = T^{-1}$ — transpose equals inverse (**orthogonal matrix**)\n\n→ Makes IDCT simple and efficient\n\n---\n\n## DCT vs DFT\n\n| | DFT | DCT |\n|--|-----|-----|\n| Uses | Cosine + Sine | Cosine only |\n| Output | Complex (real + imaginary) | Real only |\n| Compression | Energy spread out | Energy concentrated |\n\n**Why DCT is better for compression — ramp [0,1,2,3,4,5,6,7]:**\n\n```\nDCT: [9.90, -6.44,  0, -0.67, 0, -0.20, 0, -0.51]  → many zeros ✅\nDFT: [28.00, -4.00, 9.66, -4.00, 4.00, -4.00, 1.66, -4.00]  → spread ❌\n```\n\n**How DCT avoids imaginary numbers:** makes a symmetric copy of the signal → sine part cancels to zero → only cosine remains\n\n```\nOriginal:  [0, 1, 2, 3, 4, 5, 6, 7]\nSymmetric: [0, 1, 2, 3, 4, 5, 6, 7, 7, 6, 5, 4, 3, 2, 1, 0]\n```\n\n---\n\n## Orthogonal Basis Functions\n\nTwo basis functions Bₚ and Bq are **orthogonal** if:\n\n$$\\sum B_p(i) \\times B_q(i) = 0 \\quad \\text{when } p \\ne q$$\n\n**Orthonormal** = orthogonal + each has length 1:\n\n$$\\sum B_p(i) \\times B_q(i) = \\begin{cases} 0 & p \\ne q \\\\ 1 & p = q \\end{cases}$$\n\n→ DCT basis functions are orthonormal → independent → adjust one coefficient without affecting others\n\n---\n\n## JPEG Pipeline\n\n**Encoding:**\n1. Split image into **8×8 blocks**\n2. **DCT** → 64 coefficients per block\n3. **Quantize** (divide by quantization matrix, round) → small coefficients become zero ← **loss happens here**\n4. **Huffman** encode non-zero coefficients\n\n**Decoding:**\n1. Huffman decode\n2. Dequantize (multiply back)\n3. **IDCT** → reconstruct pixels\n\n**Quality control:** larger quantization values → more zeros → more compression, worse quality. This is the **JPEG quality slider**."
    }
  ],
  "flashcards": [
    {
      "q": "Why does DCT use only real numbers while DFT needs complex numbers?",
      "a": "DCT makes a symmetric copy of the input signal. Symmetric signals have a zero sine component in their DFT — only the cosine part remains. That real-only cosine representation is the DCT."
    },
    {
      "q": "For the ramp [0,1,2,3,4,5,6,7]: why is DCT better than DFT for compression?",
      "a": "DCT coefficients: [9.90,−6.44,0,−0.67,...] — many zeros, energy in first 2 terms. DFT: [28,−4,9.66,−4,4,−4,1.66,−4] — energy spread out. Using only 3 DCT terms gives a good ramp. Using 3 DFT terms oscillates badly. DCT concentrates energy → fewer terms needed → better compression."
    },
    {
      "q": "What does 'the 2D DCT is separable' mean? Why does it matter?",
      "a": "You can compute 2D DCT as two 1D DCTs: first across rows, then across columns. Reduces from 64 iterations to 8+8=16. This is why real-time video compression is computationally feasible."
    },
    {
      "q": "What is the matrix form of the DCT? What about IDCT?",
      "a": "DCT: F = T·f·T^T. IDCT: f = T^T·F·T. T is the DCT matrix, T^T is its transpose. The IDCT is just transposing T — no need for a full matrix inverse."
    },
    {
      "q": "Why does T^T = T^(-1)? What property causes this?",
      "a": "The DCT basis functions are **orthonormal** — orthogonal (different functions don't interfere) AND each has length 1. This makes T an orthogonal matrix, where the transpose equals the inverse by definition."
    },
    {
      "q": "What is the lossy step in JPEG? What controls quality?",
      "a": "The lossy step is **quantization**: each DCT coefficient is divided by a quantization value and rounded. Small coefficients become zero. Larger quantization values → more zeros → higher compression → lower quality. The quantization values form a quantization matrix — different for each frequency."
    },
    {
      "q": "Describe the complete JPEG encode/decode pipeline.",
      "a": "Encode: split into 8×8 blocks → 2D DCT → quantize (lossy, zeros small coefficients) → Huffman encode.\nDecode: Huffman decode → dequantize → IDCT → assemble blocks.\nThe quantization step is irreversible — zeroed coefficients are lost forever."
    }
  ],
  "problems": [
    {
      "type": "conceptual",
      "q": "In the 2D DCT coefficient grid (8×8), where is the DC component and where are the highest-frequency components?",
      "a": "**DC component:** top-left corner (0,0) — represents the average brightness of the 8×8 block.\n\n**Highest frequency:** bottom-right corner (7,7) — represents the fastest variation in both horizontal and vertical directions.\n\nMoving right increases horizontal frequency; moving down increases vertical frequency.",
      "hint": "DC = (0,0) = average. Frequency increases as you move right and down."
    },
    {
      "type": "conceptual",
      "q": "What does the property T^T = T^{-1} mean, and why is it useful for IDCT?",
      "a": "T is an **orthogonal matrix** — its transpose is also its inverse.\n\nThis means:\n* **DCT:** F = T × f × T^T\n* **IDCT:** f = T^T × F × T\n\nThe inverse transform uses the same matrix T — just transposed. No need to invert T separately, which makes the IDCT computationally simple and efficient.",
      "hint": "Orthogonal: T^T = T^{-1}. So the inverse transform is just swapping T and T^T."
    },
    {
      "type": "conceptual",
      "q": "In JPEG, where exactly does the lossy part happen? How does the quality slider work?",
      "a": "Loss happens during **quantization** — DCT coefficients are divided by a quantization matrix and rounded to integers. Small coefficients round to **zero** and are discarded.\n\nThe **quality slider** scales the quantization matrix:\n* High quality → small divisors → fewer zeros → more data → larger file\n* Low quality → large divisors → many zeros → less data → smaller file\n\nDCT and Huffman coding are lossless — only quantization introduces error.",
      "hint": "DCT is reversible. Huffman is reversible. Only quantization (round/discard) is irreversible."
    },
    {
      "type": "conceptual",
      "q": "Why does DCT concentrate energy better than DFT for compression?",
      "a": "DCT mirrors the signal symmetrically before transforming, which makes the signal appear **periodic without discontinuities** at the boundaries.\n\nDFT treats the signal as periodic, creating sharp edges at the boundaries → energy spreads into many high-frequency components.\n\nDCT avoids this → energy concentrates in fewer low-frequency coefficients → more zeros → better compression.\n\nIn the ramp example: DCT gives 5 near-zero values out of 8; DFT gives none.",
      "hint": "Symmetric extension → no boundary discontinuity → no energy spread → concentrated coefficients."
    }
  ]
}